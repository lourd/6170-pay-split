<div class="back-buttons">
	<%= button_to @event, :class => 'my-btn gr-btn back-btn', :method => :get do %>
		Back to event
	<% end %>

	<%= button_to events_path, :class => 'my-btn gr-btn back-btn', :method => :get do %>
		All events
	<% end %>
</div>

<div class="new-event-container">
	<h1>Editing event</h1>
	<%= render 'form' %>
</div>
